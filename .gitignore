# Python
__pycache__/
*.py[cod]
*$py.class
*.so
*.egg-info/
dist/
build/
.eggs/
*.egg

# Virtual environments
venv/
.venv/
env/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Environment
.env
.env.local

# Node
node_modules/

# OS
.DS_Store
Thumbs.db

# Logs
*.log
data/logs/

# Database
*.db
__pycache__/
*.pyc
data/*.db
# SQLite WAL/shm journal files — runtime artifacts, never commit.
*.db-wal
*.db-shm
data/*.db-wal
data/*.db-shm
data/odds_api_key.txt
data/opening_odds.json
# Hard Rock per-user overrides: session cookie + device tokens.
# Generated by scrapers/hardrock_odds.py --load-curl. Never commit.
data/hardrock_query.json
data/hardrock_headers.json
# HR market-discovery dumps from scrapers/hardrock_discover.py — large
# JSON snapshots used to inspect new market types. Local only.
data/hr_discover/
node_modules/
.env
# Auto-placer arm script — contains the HR relay token. Never commit.
# Use arm.bat.example as the template; copy to arm.bat and fill in.
arm.bat
data/queue_placer/

# ── Training/calibration outputs — Beelink is the source of truth ──
# The Beelink retrains locally via weekly_retrain.bat. Committing dev-box
# copies would overwrite Beelink-fresh models on next `git pull`. Initial
# Beelink deploy still robocopies data/ per docs/DEPLOY_TO_BEELINK.md.
#
# Note: data/models/*.json was tracked historically (700+ files) and stays
# tracked for now — untracking is a separate operation that requires the
# Beelink to back up its live copy first so `git pull` doesn't delete them.
# In the meantime, if the dev box retrains and produces diffs under
# data/models/, run `git checkout HEAD -- data/models/` to discard the
# dev-side changes before committing anything else.
*.pkl
data/*/*_gbm/
data/soccer/*/v31_brier.json
data/*_calibration.json
data/*_constants.json
data/*/*_calibration.json
data/*/*_constants.json
data/state_mc_*.json
data/blend_calibration.json
data/closing_line_weights.json
data/edge_floors.json
data/prop_shrinkage.json
data/stacker_weights.json
data/tennis_hr_cache.json
data/hardrock_sports_query.json
